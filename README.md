# Path Planning Transformer

This repository contains the implementation of a Transformer-based
path-planning framework for autonomous navigation on 2D grid maps.

## Repository Contents

- `training_and_evaluation.ipynb` — Model definition, training, and evaluation.
- `requirements.txt` — Python dependencies required to run the implementation.

## Installation

Clone the repository and install the required dependencies:

```bash
pip install -r requirements.txt
